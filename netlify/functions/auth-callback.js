import { serverError } from "./_shared/response.js";

/**
 * TODO:
 * Реализовать:
 * 1. exchangeCodeForToken(code)
 * 2. fetchDiscordUser(accessToken)
 * 3. fetchDiscordGuilds(accessToken)
 * 4. set session cookie
 */
export const handler = async () => {
  return serverError("auth-callback not implemented yet");
};
